SESSION_SECRET=change-me-before-deploying
TEACHER_PASSWORD=choose-a-strong-teacher-password

# Optional server-side OpenAI challenge builder. Keep this secret server-side.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-sol

# Optional production runner boundary. If set, the web app sends judging jobs here
# instead of using the local development subprocess adapter.
RUNNER_SERVICE_URL=
